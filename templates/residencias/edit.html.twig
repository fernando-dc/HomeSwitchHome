{% extends 'base.html.twig' %}

{% block title %}Edit Residencias{% endblock %}

{% block body %}
    <h1>Edit Residencias</h1>

    {{ include('residencias/_form.html.twig', {'button_label': 'Guardar cambios'}) }}

    <a href="{{ path('residencias_index') }}">Atras</a>

    {{ include('residencias/_delete_form.html.twig') }}
{% endblock %}
