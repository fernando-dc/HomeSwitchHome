{% extends 'base.html.twig' %}

{% block title %}Residencias{% endblock %}

{% block body %}
    <h1>Residencias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdResidencia</th>
                <td>{{ residencia.idResidencia }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ residencia.tipo }}</td>
            </tr>
            <tr>
                <th>Habitaciones</th>
                <td>{{ residencia.habitaciones }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ residencia.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('residencias_index') }}">back to list</a>

    <a href="{{ path('residencias_edit', {'idResidencia': residencia.idResidencia}) }}">edit</a>

    {{ include('residencias/_delete_form.html.twig') }}
{% endblock %}
